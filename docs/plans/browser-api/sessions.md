# BrowserApi — Session Tracker

**Parent:** [browser-api.md](browser-api.md)

Each session is scoped to fit within a single Claude Code context window. Sessions are ordered — later sessions depend on earlier ones. Check off sessions as they're completed and note any deviations in the archive.

---

## Phase 1: Foundation

### Session 1.1 — Download pre-parsed IDL JSON + core primitives
- [ ] Download `ed/idlparsed/*.json` from webref into `specs/idlparsed/`
- [ ] Create `ICssValue` interface (`ToCss()` → string)
- [ ] Create `IWebIdlSerializable` interface (`ToJs()` → object)
- [ ] Create `StringValueAttribute` for mapping C# enum values to WebIDL strings
- [ ] Create `JsNameAttribute` for preserving original JS member names
- [ ] Write unit tests for attributes and interfaces
- [ ] Verify build passes

### Session 1.2 — Generator: JSON deserialization + internal AST model
- [ ] Define internal AST model classes (IdlInterface, IdlMember, IdlType, IdlEnum, IdlDictionary, etc.)
- [ ] Define `ISpecReader` interface (the seam for swappable input modules)
- [ ] Implement `WebRefJsonReader` — deserialize webref idlparsed JSON into AST model
- [ ] Test with a small spec (e.g., `console.json`) — assert all members, types, and attributes are parsed
- [ ] Test with a medium spec (e.g., `fetch.json`) — assert partials, mixins, `includes` are captured

### Session 1.3 — Generator: cross-reference resolver
- [ ] Build resolver that merges `partial interface` definitions from `idlExtendedNames` into primary definitions
- [ ] Resolve `includes` statements — copy mixin members into target interfaces
- [ ] Build inheritance chain (interface A extends B)
- [ ] Resolve `typedef` references
- [ ] Handle `externalDependencies` — load definitions from other spec JSON files as needed
- [ ] Test: given `fetch.json` + `dom.json`, assert `Request` includes `Body` mixin members
- [ ] Test: given `html.json`, assert partial interface definitions are merged

### Session 1.4 — Generator: C# emitter (interfaces + enums)
- [ ] AST → C# model transform (PascalCase naming, type mapping, async detection)
- [ ] Emit interfaces as `partial class` with properties and method signatures
- [ ] Emit `readonly attribute` as get-only properties
- [ ] Emit enums with `[StringValue]` attributes
- [ ] Emit `// <auto-generated/>` header with source spec name
- [ ] Snapshot tests: given AST, assert exact C# output
- [ ] Test end-to-end: `console.json` → resolver → emitter → valid C# file

### Session 1.5 — Generator: C# emitter (dictionaries, callbacks, full pipeline)
- [ ] Emit dictionaries as `record class`
- [ ] Emit callbacks as `delegate` / `Action<>` / `Func<>`
- [ ] Emit `Promise<T>` → `Task<T>` with `Async` suffix
- [ ] Emit `sequence<T>` → `IReadOnlyList<T>`
- [ ] Handle union types → method overloads
- [ ] Handle nullable types
- [ ] Wire up CLI (`--idlparsed`, `--css-data`, `--output` args)
- [ ] End-to-end test: run generator on a handful of real specs, verify output compiles

### Session 1.6 — Generator: CSS data parser + CSS property emission
- [ ] Parse CSS JSON files (`name`, `value` grammar, `initial`, `inherited`)
- [ ] Map common CSS value grammars to C# types (start with hand-curated mapping)
- [ ] Emit `CssStyleDeclaration` partial class with typed properties
- [ ] Emit CSS value type stubs implementing `ICssValue`
- [ ] Test: parse `css-flexbox.json`, assert correct properties emitted

---

## Phase 2: CSS Types

### Session 2.1 — Core CSS value types (Length, Color)
- [ ] `Length` struct: Px, Em, Rem, Vh, Vw, Percent, Calc — with `ToCss()` and factory methods
- [ ] `CssColor` struct: Named, Hex, Rgb, Rgba, Hsl, Hsla — with `ToCss()` and factories
- [ ] TDD: `Assert.Equal("1.5rem", Length.Rem(1.5).ToCss())`
- [ ] TDD: `Assert.Equal("hsl(220, 90%, 56%)", CssColor.Hsl(220, 90, 56).ToCss())`

### Session 2.2 — More CSS value types (Duration, Angle, Percentage, etc.)
- [ ] `Duration` (s, ms)
- [ ] `Angle` (deg, rad, grad, turn)
- [ ] `Percentage`
- [ ] `Resolution` (dpi, dpcm, dppx)
- [ ] `Flex` (fr)
- [ ] All with `ToCss()`, factory methods, unit tests

### Session 2.3 — Complex CSS types (Transform, Gradient, Shadow, Transition)
- [ ] `Transform` with fluent chaining: `Transform.Rotate(45.Deg()).Scale(1.5)`
- [ ] `Gradient` (linear, radial, conic)
- [ ] `Shadow` (box-shadow, text-shadow)
- [ ] `Transition` with property, duration, timing, delay
- [ ] Unit tests for all `ToCss()` outputs

### Session 2.4 — CssStyleDeclaration ergonomics
- [ ] Review generated `CssStyleDeclaration` — ensure properties use correct value types
- [ ] Add operator overloads where useful (implicit conversion from numeric to Length, etc.)
- [ ] Ensure shorthand properties work (margin, padding, border)
- [ ] Integration tests: build a style declaration, assert full CSS output

---

## Phase 3: DOM Types

### Session 3.1 — Run generator on DOM + HTML specs
- [ ] Run generator on `dom.json`, `html.json` — get the full interface hierarchy generated
- [ ] Fix any generator issues found with these large, complex specs
- [ ] Verify: EventTarget, Node, Element, HTMLElement all generate correctly
- [ ] Verify: partial interfaces from html.json merge properly

### Session 3.2 — DOM type refinements + event hierarchy
- [ ] Run generator on event-related specs
- [ ] Verify event type hierarchy: Event → UIEvent → MouseEvent, KeyboardEvent, etc.
- [ ] Hand-write ergonomic additions: `Key` enum, `Modifiers` flags, `MouseButtons` flags
- [ ] Add event registration pattern (typed add/remove accessors on Element)

### Session 3.3 — HTML element coverage
- [ ] Verify all HTML element types generate (HTMLDivElement, HTMLInputElement, etc.)
- [ ] Review generated types for correctness — spot-check 10-15 elements
- [ ] Fix any generator issues with element-specific attributes
- [ ] Compilation test: all generated DOM types build cleanly

---

## Phase 4: Interop Backend

### Session 4.1 — IBrowserBackend + JsObject base class
- [ ] `IBrowserBackend` interface (get property, set property, invoke method, subscribe event)
- [ ] `JsObject` base class wrapping a JS object reference
- [ ] Property get/set via backend
- [ ] Method invoke via backend (sync and async)
- [ ] Unit tests with mock backend

### Session 4.2 — JSInteropBackend implementation
- [ ] Implement `IBrowserBackend` using `IJSRuntime`
- [ ] Wire up `InvokeAsync` for method calls
- [ ] Wire up property access via JS eval or helper functions
- [ ] Event subscription via `addEventListener` / `removeEventListener`
- [ ] JS-side helper script for property access and event bridging

---

## Phase 5: Blazor Integration

### Session 5.1 — DI + component base classes
- [ ] `AddBrowserApi()` service collection extension
- [ ] Component base class with backend injection
- [ ] Lifecycle hooks (OnAfterRenderAsync for JS interop init)
- [ ] Sample component demonstrating usage

---

## Phase 6: Additional APIs

### Session 6.1 — Canvas 2D
- [ ] Run generator on canvas spec
- [ ] Hand-write fluent path building, save/restore pattern
- [ ] Typed fill/stroke overloads, font builder, gradient builder

### Session 6.2 — Fetch API
- [ ] Run generator on fetch spec
- [ ] Hand-write fluent request builder, generic deserialization
- [ ] Result-based error handling

### Session 6.3 — Storage API
- [ ] Run generator on storage specs
- [ ] Hand-write `TypedStorage<T>` generic wrapper
- [ ] Reactive `OnChanged` event

### Session 6.4 — Web Animations API
- [ ] Run generator on web-animations spec
- [ ] Hand-write keyframe builder, typed AnimationOptions
- [ ] Easing functions

---

## Status Key

- `[ ]` — Not started
- `[~]` — In progress
- `[x]` — Completed
- `[!]` — Blocked (note reason)
- `[-]` — Skipped (note reason)

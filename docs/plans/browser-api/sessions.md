# BrowserApi — Session Tracker

**Parent:** [browser-api.md](browser-api.md)

Each session is scoped to fit within a single Claude Code context window. Sessions are ordered — later sessions depend on earlier ones. Check off sessions as they're completed and note any deviations in the archive.

---

## Phase 1: Foundation

### Session 1.1 — Download pre-parsed IDL JSON + core primitives
- [x] Download `ed/idlparsed/*.json` from webref into `specs/idlparsed/`
- [x] Create `ICssValue` interface (`ToCss()` → string)
- [x] Create `IWebIdlSerializable` interface (`ToJs()` → object)
- [x] Create `StringValueAttribute` for mapping C# enum values to WebIDL strings
- [x] Create `JsNameAttribute` for preserving original JS member names
- [x] Write unit tests for attributes and interfaces
- [x] Verify build passes

### Session 1.2 — Generator: JSON deserialization + internal AST model
- [x] Define internal AST model classes (IdlInterface, IdlMember, IdlType, IdlEnum, IdlDictionary, etc.)
- [x] Define `ISpecReader` interface (the seam for swappable input modules)
- [x] Implement `WebRefJsonReader` — deserialize webref idlparsed JSON into AST model
- [x] Test with a small spec (e.g., `console.json`) — assert all members, types, and attributes are parsed
- [x] Test with a medium spec (e.g., `fetch.json`) — assert partials, mixins, `includes` are captured

### Session 1.3 — Generator: cross-reference resolver
- [x] Build resolver that merges `partial interface` definitions from `idlExtendedNames` into primary definitions
- [x] Resolve `includes` statements — copy mixin members into target interfaces
- [x] Build inheritance chain (interface A extends B)
- [x] Resolve `typedef` references
- [x] Handle `externalDependencies` — load definitions from other spec JSON files as needed
- [x] Test: given `fetch.json` + `dom.json`, assert `Request` includes `Body` mixin members
- [x] Test: given `html.json`, assert partial interface definitions are merged

### Session 1.4 — Generator: C# emitter (interfaces + enums)
- [x] AST → C# model transform (PascalCase naming, type mapping, async detection)
- [x] Emit interfaces as `partial class` with properties and method signatures
- [x] Emit `readonly attribute` as get-only properties
- [x] Emit enums with `[StringValue]` attributes
- [x] Emit `// <auto-generated/>` header with source spec name
- [x] Snapshot tests: given AST, assert exact C# output
- [x] Test end-to-end: `console.json` → resolver → emitter → valid C# file

### Session 1.5 — Generator: C# emitter (dictionaries, callbacks, full pipeline)
- [x] Emit dictionaries as `record class`
- [x] Emit callbacks as `delegate` / `Action<>` / `Func<>`
- [x] Emit `Promise<T>` → `Task<T>` with `Async` suffix
- [x] Emit `sequence<T>` → `IReadOnlyList<T>`
- [x] Handle union types → method overloads
- [x] Handle nullable types
- [x] Wire up CLI (`--idlparsed`, `--css-data`, `--output` args)
- [x] End-to-end test: run generator on a handful of real specs, verify output compiles

### Session 1.6 — Generator: CSS data parser + CSS property emission
- [x] Parse CSS JSON files (`name`, `value` grammar, `initial`, `inherited`)
- [x] Map common CSS value grammars to C# types (start with hand-curated mapping)
- [x] Emit `CssStyleDeclaration` partial class with typed properties
- [x] Emit CSS value type stubs implementing `ICssValue`
- [x] Test: parse `css-flexbox.json`, assert correct properties emitted

---

## Phase 2: CSS Types

### Session 2.1 — Core CSS value types (Length, Color)
- [ ] `Length` struct: Px, Em, Rem, Vh, Vw, Percent, Calc — with `ToCss()` and factory methods
- [ ] `CssColor` struct: Named, Hex, Rgb, Rgba, Hsl, Hsla — with `ToCss()` and factories
- [ ] TDD: `Assert.Equal("1.5rem", Length.Rem(1.5).ToCss())`
- [ ] TDD: `Assert.Equal("hsl(220, 90%, 56%)", CssColor.Hsl(220, 90, 56).ToCss())`

### Session 2.2 — More CSS value types (Duration, Angle, Percentage, etc.)
- [ ] `Duration` (s, ms)
- [ ] `Angle` (deg, rad, grad, turn)
- [ ] `Percentage`
- [ ] `Resolution` (dpi, dpcm, dppx)
- [ ] `Flex` (fr)
- [ ] All with `ToCss()`, factory methods, unit tests

### Session 2.3 — Complex CSS types (Transform, Gradient, Shadow, Transition)
- [ ] `Transform` with fluent chaining: `Transform.Rotate(45.Deg()).Scale(1.5)`
- [ ] `Gradient` (linear, radial, conic)
- [ ] `Shadow` (box-shadow, text-shadow)
- [ ] `Transition` with property, duration, timing, delay
- [ ] Unit tests for all `ToCss()` outputs

### Session 2.4 — CssStyleDeclaration ergonomics
- [ ] Review generated `CssStyleDeclaration` — ensure properties use correct value types
- [ ] Add operator overloads where useful (implicit conversion from numeric to Length, etc.)
- [ ] Ensure shorthand properties work (margin, padding, border)
- [ ] Integration tests: build a style declaration, assert full CSS output

---

## Phase 3: DOM Types

### Session 3.1 — Run generator on DOM + HTML specs
- [ ] Run generator on `dom.json`, `html.json` — get the full interface hierarchy generated
- [ ] Fix any generator issues found with these large, complex specs
- [ ] Verify: EventTarget, Node, Element, HTMLElement all generate correctly
- [ ] Verify: partial interfaces from html.json merge properly

### Session 3.2 — DOM type refinements + event hierarchy
- [ ] Run generator on event-related specs
- [ ] Verify event type hierarchy: Event → UIEvent → MouseEvent, KeyboardEvent, etc.
- [ ] Hand-write ergonomic additions: `Key` enum, `Modifiers` flags, `MouseButtons` flags
- [ ] Add event registration pattern (typed add/remove accessors on Element)

### Session 3.3 — HTML element coverage
- [ ] Verify all HTML element types generate (HTMLDivElement, HTMLInputElement, etc.)
- [ ] Review generated types for correctness — spot-check 10-15 elements
- [ ] Fix any generator issues with element-specific attributes
- [ ] Compilation test: all generated DOM types build cleanly

---

## Phase 4: Interop Backend

### Session 4.1+4.2 — IBrowserBackend + JsObject + JSInteropBackend (combined)
- [x] `JsHandle` opaque wrapper struct in `src/BrowserApi/Common/`
- [x] `IBrowserBackend` interface (get/set property, invoke sync/async, construct, events)
- [x] `JsObject` base class with delegation helpers + type conversion (ConvertToJs/ConvertFromJs)
- [x] `InternalsVisibleTo` for JSInterop and Tests projects
- [x] Generator: ClassEmitter emits delegation bodies (GetProperty, SetProperty, Invoke, InvokeVoid, InvokeAsync, InvokeVoidAsync)
- [x] Generator: IdlToCSharpTransformer injects JsObject base class + always sets JsName
- [x] Fixed CSS emitter file collision (CssStyleDeclaration.CssProperties.g.cs)
- [x] Regenerated all 2,552 types with delegation bodies
- [x] `MockBrowserBackend` for testing without a browser
- [x] `JsObjectTests` — property/method delegation, type conversion, JsHandle equality
- [x] `InteropIntegrationTests` — Node.AppendChild, Element.GetAttribute/SetAttribute, property delegation
- [x] Updated `ClassEmitterTests` — all assertions updated for new delegation output
- [x] Updated `DomHierarchyTests` — added EventTarget extends JsObject assertion
- [x] `JSInteropBackend` using `IJSRuntime`/`IJSInProcessRuntime`
- [x] `browserapi.js` helper module (getProperty, setProperty, invoke, construct, addEventListener)
- [x] JSInterop csproj switched to Razor SDK for static web asset embedding
- [x] Build: 0 warnings, 0 errors
- [x] Tests: 418 passing (126 generator + 292 core)

---

## Phase 5: Blazor Integration

### Session 5.1 — DI + component base classes
- [x] `AddBrowserApi()` service collection extension (registers `JSInteropBackend` as scoped)
- [x] `BrowserApiComponentBase` — abstract component with `IJSRuntime` injection
- [x] Lifecycle hooks: `OnAfterRenderAsync(firstRender)` creates backend, sets `JsObject.Backend`, obtains `Window`/`Document` via `GetGlobal()`
- [x] Virtual `OnBrowserApiReadyAsync()` hook for subclass DOM init
- [x] `InternalsVisibleTo` for `BrowserApi.Blazor` (enables `Handle` setting)
- [x] `MockJSInProcessRuntime` for testing without a browser
- [x] Tests: DI registration, component lifecycle, pre-init guards, dispose
- [x] Build: 0 warnings, 0 errors
- [x] Tests: 427 passing (126 generator + 301 core/Blazor)

---

## Phase 6: Additional APIs

### Session 6.1 — Canvas 2D
- [x] Generated types already in place: CanvasRenderingContext2D (40+ methods), Path2D, CanvasGradient, CanvasPattern, TextMetrics, ImageData
- [x] `CanvasStateScope` — save/restore via `using (ctx.SaveState()) { ... }`
- [x] `CanvasExtensions` — typed `SetFill`/`SetStroke` with CssColor/Gradient/Pattern, `SetShadow`, `SetLineStyle`, fluent `Path()` and gradient builder entry points
- [x] `PathBuilder` — fluent path building: `ctx.Path().MoveTo().LineTo().ClosePath().Fill()`
- [x] `GradientBuilder` — fluent gradient: `ctx.LinearGradient().AddStop(0, color).Build()`
- [x] `CanvasFont` — font value type with `Of(size, family).Bold().Italic()`, implicit string conversion
- [x] Tests: 34 new Canvas tests (font, extensions, path builder, gradient builder)
- [x] Build: 0 warnings, 0 errors
- [x] Tests: 461 passing (126 generator + 335 core)

### Session 6.2 — Fetch API
- [x] Generated types already in place: Request, Response, Headers, RequestInit, AbortController/Signal
- [x] `Http` static entry point — `Http.Get(url)`, `Http.Post(url)`, `Http.GetAsync<T>(url)` shortcut
- [x] `RequestBuilder` — fluent builder: `WithHeader`, `WithJsonBody`, `WithCredentials`, `WithMode`, `WithSignal`, `SendAsync`, `SendJsonAsync<T>`, `TrySendAsync`, `TrySendAsync<T>`
- [x] `FetchResult` / `FetchResult<T>` — result types for no-throw error handling
- [x] `ResponseExtensions` — `JsonAsync<T>()` generic deserialization, `EnsureSuccess()`
- [x] Tests: 17 new Fetch tests (builder chaining, result types, response extensions)
- [x] Build: 0 warnings, 0 errors
- [x] Tests: 478 passing (126 generator + 352 core)

### Session 6.3 — Storage API
- [x] Generated types already in place: Storage, StorageEvent, StorageManager, IndexedDB (50+ files)
- [x] `TypedStorage` wrapper — `Get<T>`, `Set<T>` with JSON serialization, `GetString`/`SetString` raw access, `ContainsKey`, `Count`, `Keys`
- [x] `StorageExtensions` on Window — `TypedLocalStorage()`, `TypedSessionStorage()`, `OnStorageChanged(callback)`
- [x] `StorageChangedEventArgs` — typed event args for cross-tab storage changes
- [x] Namespace: `BrowserApi.WebStorage` (avoids collision with generated `Storage` class)
- [x] Tests: 13 new Storage tests (typed get/set, raw access, containsKey, count, extensions)
- [x] Build: 0 warnings, 0 errors
- [x] Tests: 491 passing (126 generator + 365 core)

### Session 6.4 — Web Animations API
- [ ] Run generator on web-animations spec
- [ ] Hand-write keyframe builder, typed AnimationOptions
- [ ] Easing functions

---

## Status Key

- `[ ]` — Not started
- `[~]` — In progress
- `[x]` — Completed
- `[!]` — Blocked (note reason)
- `[-]` — Skipped (note reason)
